function hello(name) {
if(name == undefined){
   return "Hello, World!";
   }
if(name == ""){
return "Hello, World!"; 
}

  return "Hello, " + name.charAt(0).toUpperCase() + name.toLowerCase().slice(1) + "!";
  
}
