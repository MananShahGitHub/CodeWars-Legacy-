function isNarcissistic(n){
  var num = n.toString();
  var pow = 0;
  for (var i = 0; i < num.length; i++) {
  pow += Math.pow(+num[i], num.length)
  }
  if(pow === n){
     return true;
  }
  else{
  return false;
  }
}
