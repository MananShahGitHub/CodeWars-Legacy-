function twiceAsOld(dadYearsOld, sonYearsOld) {
  var count=0;
  if (dadYearsOld>=sonYearsOld*2)
    while (dadYearsOld+count>(sonYearsOld+count)*2)
      count++;
  else
    while (dadYearsOld-count<(sonYearsOld-count)*2)
      count++;
  return count;
}
